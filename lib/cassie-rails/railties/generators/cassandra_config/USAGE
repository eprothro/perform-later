Description:
    Generates a default cassandra database configuration for use by cassie gem

Example:
    rails generate cassandra_config

    This will create:
      config/cassandra.yml

    Inside this file a default configuration will be ready for completion.

